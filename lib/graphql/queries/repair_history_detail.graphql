query repair_history_detail($requestID: String!){
  ds_request_conflicts(filters: {
    column: "id",
    condition: equals,
    value: $requestID,
  }){
    id,
    name,
    fixer_description,
    fixer_id,
    fixer_image,
    ended_at,
    status_id,
    status_name,
  }
}