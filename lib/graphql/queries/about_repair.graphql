query repair_history{
  ds_request_conflicts(filters: {
    column: "status_id",
    condition: equals,
    value: "3"
  }){
    id
    fixer_id,
    fixer_description,
    status_id,
    status_name,
    }    
}